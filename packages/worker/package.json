{
  "name": "@mailsink/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run && pnpm run test:smoke:local",
    "test:smoke:local": "node --test test/local-email-smoke.mjs",
    "typecheck": "tsc --noEmit",
    "deploy": "npx wrangler deploy",
    "dev": "npx wrangler dev"
  },
  "dependencies": {
    "@mailsink/shared": "workspace:*",
    "postal-mime": "^2.4.3",
    "ulidx": "^2.4.1"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.8",
    "@cloudflare/workers-types": "5.20260722.1",
    "@types/node": "^24.0.0",
    "typescript": "^5.8.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.115.0"
  }
}
